# Backend host port for the normal containerized app
APP_PORT=8080

# Backend host port for live-reload dev mode
APP_DEV_PORT=8081

# MySQL root password used by both db and backend services
DB_ROOT_PASSWORD=gLp(43<Ze1d9
